// import { createParamDecorator, ExecutionContext } from '@nestjs/common';
//
// export const ConnectedUser = createParamDecorator(
//   (_, contexte: ExecutionContext) =>
//     contexte.switchToHttp().getRequest().user ?? null,
// );
